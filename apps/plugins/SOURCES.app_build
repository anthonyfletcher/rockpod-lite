#if !defined(HAVE_TOUCHSCREEN) || defined(DX50) || defined(DX90) || defined(HIBY_R3PROII) || defined(HIBY_R1)
/* In devices running RockBox as an application, but having a keypad */
#include "SOURCES"
#else
/* plugins common to all models -- trimmed to the iPod 6G/5G DAP-only
 * utility set. Not on this fork's actual build path (iPod targets never
 * set APP_TYPE), kept only for consistency with SOURCES. */
credits.c
properties.c
search.c
vbrfix.c
view_text.c

#endif /* HAVE_TOUCHSCREEN */
