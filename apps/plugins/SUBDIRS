/* No subdirectory plugins remain. The viewers were all retired in favour of
 * core apps:
 *   - text_viewer -> apps/text_viewer/ (the license/legal screens in
 *     apps/menus/main_menu.c call the core text_viewer() directly, and the
 *     file browser reaches it via filetype_open_core_viewer()).
 *   - imageviewer -> apps/image_viewer/ (album art + file browser).
 *   - mpegplayer / shortcuts / clock / tagcache: removed.
 * db_folder_select -> apps/folder_select.c (the DB "Directories to Scan"
 * setting and the custom autoresume folder picker call folder_select()
 * directly). No plugins remain; SOURCES lists none. */
