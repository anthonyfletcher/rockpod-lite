/* No plugins remain -- everything here was either a standalone utility only
 * reachable through the removed Plugins browser, or has been folded into the
 * core:
 * db_folder_select.c moved into the core (apps/folder_select.c), called
 * directly from apps/menus/settings_menu.c's "Directories to Scan" database
 * setting and the AUTORESUME_NEXTTRACK_CUSTOM folder picker -- no longer a
 * plugin.
 * properties.c moved into the core (apps/properties.c, plus apps/mul_id3.c),
 * reached directly from apps/onplay.c's properties_item/track_info_item menu
 * entries and the HOTKEY_PROPERTIES hotkey -- no longer a plugin.
 * playing_time.c moved into the core (apps/playing_time.c), reached directly
 * from apps/onplay.c's "Playing Time" current-playlist menu item -- no longer
 * a plugin.
 * lastfm_scrobbler.c removed entirely: play logging is core (apps/playback.c),
 * and the "play log" setting is now a 3-way Off / On / Audioscrobbler choice
 * that writes /.scrobbler.log directly -- no plugin, no export step.
 * credits.c moved into the core (apps/credits.c), launched directly from
 * apps/menus/main_menu.c's show_credits() -- no longer a plugin.
 * Everything else previously here (games, demos, emulators, remaining
 * viewers/utilities only ever reachable through the generic Plugins
 * browser) has been removed. main_menu_config.c and pictureflow.c
 * specifically: both superseded by core ports (apps/menus/
 * main_menu_config.c, apps/gui/album_covers.c) earlier this fork -- not
 * part of this trim, just finally deleted as dead weight now that this
 * file was being edited anyway.
 * keyremap.c removed too: it was only ever reachable through the generic
 * Plugins browser, same as the games/demos/emulators above -- no core
 * call site ever plugin_load()ed it directly. */
