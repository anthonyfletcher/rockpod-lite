/* plugins common to all models -- trimmed to the iPod 6G/5G DAP-only
 * utility set. Everything else (games, demos, emulators, extra viewers)
 * has been removed from this fork. */
#ifdef HAVE_TAGCACHE
db_folder_select.c
#endif
credits.c
file_picker.c
keyremap.c
lastfm_scrobbler.c
lastfm_scrobbler_viewer.c
main_menu_config.c
open_plugins.c
properties.c
search.c
view_text.c
vbrfix.c
disktidy.c

/* Overlay loader -- required alongside apps/plugins/pictureflow/ */
#if PLUGIN_BUFFER_SIZE <= 0x20000
pictureflow.c
#endif /* PLUGIN_BUFFER_SIZE <= 0x20000 */

#if CONFIG_RTC
alarmclock.c
#endif /* CONFIG_RTC */

/* can't build in the sim for some reason */
#if defined(HAVE_MULTIBOOT) && !defined(SIMULATOR)
multiboot_select.c
#endif
